#include "Game.h"

#include <map>

void Game::init_transformation_rules() {
    production_rule p1;

    for (auto non_terminal : m_grammar.) {

    }
}

double Game::evaluated_expression(Expression &expr) {
    return 0;
}

bool Game::is_terminal(Expression &expr) {
    return false;
}
